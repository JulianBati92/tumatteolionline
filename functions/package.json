{
  "name": "functions",
  "description": "Firebase Functions for payment checkout",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check": "node --check index.js",
    "audit": "npm audit --omit=dev"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2",
    "stripe": "^22.6.0"
  }
}
